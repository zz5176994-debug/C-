# 类型  
## 1. 整数  
* **char , short , int , long , long long**(C99)
  
| 类型                  | 格式化输出 printf | 格式化输入 scanf | 取值范围（典型32位系统）                            | 大小 (字节) | 内存表达方式                          |
|-----------------------|--------------------|-------------------|------------------------------------------------------|-------------|----------------------------------------|
| `char`                | `%c` / `%d`        | `%c` / `%hhd`     | -128 ~ 127（有符号）(即-$2^{(n-1)}$ ~ $2^{n-1}-1$) / 0 ~ 255（无符号）            | 1(8比特)           | 补码存储，通常为小端模式               |
| `short`               | `%hd`              | `%hd`             | -32768 ~ 32767                                      | 2           | 补码，小端存储                         |
| `int`                 | `%d`               | `%d`              | -2,147,483,648 ~ 2,147,483,647  (即-2^(32-1)~2^(32-1)-1)                     | 4（int是用来表达寄存器的）        | 补码，小端存储                         |
| `long`                | `%ld`              | `%ld`             | 常见为32位：同int，64位系统为：-9e18~9e18          | 4 / 8       | 补码，小端存储                         |
| `long long`           | `%lld`             | `%lld`            | -9,223,372,036,854,775,808 ~ 9,223,372,036...       | 8           | 补码，小端存储                         |
| `unsigned long`           | `%lu`             | `%lu`            | ///       | /           | /                         |  

- 例子  

```c
#include<stdio.h>
int main()
{
  char c = -1;
  int i = -1;
  printf("c=%u,i=%u\n",c,i);//c从char的-1，由于%u,转换成int的-1，所以现在c是int的-1
  return 0;
}
```  

![](./img/%25u的输出.png)

## 2. 浮点数
2.1. * **float , double , long double**(C99)  

 | 类型     | printf 输出格式                        | scanf 输入格式 | 典型取值范围（IEEE-754，含符号）                                                                 | 大小(字节) | 有效数字                    |
|----------|----------------------------------------|----------------|---------------------------------------------------------------------------------------------------|------------|----------------------------------|
| float    | `%f`, `%e`, `%g`, `%a`（都按 double 读取） | `%f`           | 正规最小：≈ 1.17549435e−38；最大：≈ 3.40282347e+38；最小非正规：≈ 1.40129846e−45                   | 4          | 7      |
| double   | `%f`, `%e`, `%g`, `%a`                 | `%lf`          | 正规最小：≈ 2.2250738585072014e−308；最大：≈ 1.7976931348623157e+308；最小非正规：≈ 4.940656e−324 | 8          | 15     |  


其中printf--->%e可以输出科学计数法的结果  

```c
#include<stdio.h>
int main()
{
  double ff = 1234.56789;
  printf("%e,%f\n",ff,ff);

  return 0;
}
```

结果`1.234568e+003,1234.567890`  

2.2. **科学计数法**
  
![](https://cdn.jsdelivr.net/gh/zz5176994-debug/images@main/pic/%E7%A7%91%E5%AD%A6%E8%AE%A1%E6%95%B0%E6%B3%95.jpg)

需明确C语言中浮点数（科学计数法形式）的合法规则：科学计数法的格式为  `[数字部分]e[指数部分]` ，其中：
 
- 数字部分可以是整数或小数；
​
- 指数部分必须是整数（正整数或负整数）；
​
-  **e  前面必须有数字， e  后面必须有指数**。

```c
#include<stdio.h>
int main()
{
  double ff = 1E-10;
  printf("%E,%.16f\n",ff,ff);
/* 在%和f之间加上.n可以指定输出小数点后几位，这样的输出是四舍五入的 */
  return 0;
}
```  
结果`1.000000E-010,0.0000000001000000`  

```c
#include<stdio.h>
int main()
{
  printf("%.3f\n",-0.0049);
  printf("%.30f\n",-0.0049);//double精度有限
  printf("%.3f\n",-0.00049);//四舍五入
  return 0;
}
```
结果`-0.005
-0.004899999999999999800000000000
-0.000` 

2.3. - **超过范围的浮点数**  
1. printf输出inf表示超过范围的浮点数:$\pm$$\infty$  
2. printf输出nan表示不存在的浮点数   

```c
#include<stdio.h>
int main()
{
  printf("%f\n",12.0/0.0);
  printf("%f\n",-12.0/0.0);
  printf("%f\n",0.0/0.0);
  return 0;
}
```
结果`1.#INF00
-1.#INF00
-1.#IND00`  

```c
#include<stdio.h>
int main()
{
  float a,b,c;

  a = 1.345f;
  /*带小数点的字面量是double而非float
  float需要用f或F后缀来表明身份*/
  b = 1.123f;
  c = a + b;
  if(c == 2.468)
  /*两个浮点数判断相等的操作 f1 == f2 可能失败
正确的方法是fabs(f1 - f2) < 1e-12
其中fabs表示绝对值*/
    printf("相等\n");
  else
    printf("不相等！ c = %.10f,或%f\n",c,c);//

  return 0;
}
```  
结果`不相等！ c = 2.4679999352,或2.468000`  

![](https://cdn.jsdelivr.net/gh/zz5176994-debug/images@main/pic/%E6%B5%AE%E7%82%B9%E6%95%B0%E5%86%85%E9%83%A8%E8%A1%A8%E8%BE%BE.jpg) 

## 3. 字符类型  

- 最常用的字符集是ASCII (发音asskee)(美国信息交换标准码),它用7位编码表示128个字符  

- ASCII常被扩展为8位代码用于表示256个字符，被称为Latin-1,提供一些西欧和许多非洲语言所需的字符

- 字符 'a' 的值为97, 'A'的值为65,'0' 的值为48，' '(空格space)的值为32
 

char是一种**整数**，也是一种特殊类型：**字符**。这是因为:
  - 用' '表示字符字面量：'a','1',可以在scanf,printf用%c来输入输出字符
```c
/*如何输入'1'这个字符给char c?*/
#include<stdio.h>
int main()
{
  char c;
  scanf("%c",&c);//注意
  printf("c = %d\n",c);
  printf("c = '%c'\n",c);
  return 0;
}
``` 
结果`1
c = 49
c = '1'`  

```c
/*字符计算*/
#include<stdio.h>
int main()
{
  char c = 'A';
  c++;
  printf("c = '%c'\n",c);
  return 0;
}
/*字母在ASCII表中是顺序排列的
大写字母和小写字母是分开排列的，并不在一起
‘a'-'A'可以得到两段之间的距离,于是
a+'a'-'A”可以把一个大写字母变成小写字母，而
a+'A'-'a'可以把一个小写字母变成大写字母*/
```
结果`B`  

与整型类似，char存在**有符号**和**无符号**两种
① signed char 
8bits,表示范围:-128~127
②unsigned char 
8bits,表示范围:0~255  


## 4. 逻辑类型
* **bool**(C99)  

| 类型名称          | 头文件         | 本质/底层类型 | 取值范围       | 内存大小 | printf 格式化 | scanf 格式化 | 内存表示方式                       |
|-------------------|----------------|----------------|-----------------|-----------|----------------|----------------|-------------------------------------|
| `_Bool`           | 无需头文件     | 关键字         | 0 或 1          | 1 字节     | `%d` (输出0/1) | `%d`           | 0 => 所有位为0；非0赋值转为1        |
| `bool`            | `#include <stdbool.h>` | 宏，等于 `_Bool` | 0 (false), 1 (true) | 1 字节 | `%d`            | `%d`          | 实质就是 `_Bool`，编译期 typedef    |  

*4.1.* 运用bool,true,false需要先包含一个头文件`#include<stdbool.h>` :grinning:


```c
#include<stdio.h>
#include<stdbool.h>
int main()
{
  bool b = 6>5;
  bool t = true;//布尔类型中,允许对变量进行赋值,但非零整数会被转换为true(即1)，而0会被转化为false(即0)
  t = 2;
  printf("%d\n",b);
  return 0;
}
```  

结果`1`



## 5. 指针  
## 6. 自定义类型  

# 二、工具  
## 1. sizeof  
  是一个运算符，给出某个类型或变量在内存中所占据的字节数
```c
int a;
a = 6;
printf("sizeof(int) = %ld\n",sizeof(int));
printf("sizeof(a) = %ld\n",sizeof(a));

```
运行结果  
 
![](https://cdn.jsdelivr.net/gh/zz5176994-debug/images@main/pic/sizeof.png) 
    是静态运算符，在sizeof的括号里做运算，这些运算是不会运算的
```c
int a;
a = 6;
printf("sizeof(a) = %ld\n",sizeof(a++));
printf("a = %d\n",a);  //a++不会运算
```

![](https://cdn.jsdelivr.net/gh/zz5176994-debug/images@main/pic/sizeof(a%2B%2B).png)  

## 2. putchar/getchar
作用：读入单个字符，类似printf/scanf  
- putchar函数写单独一个字符: `putchar(ch);`  
-  getchar()函数原型
   `int getchar ( void );`
    1. 每次调用getchar函数将读取并返回一个字符: `ch = getchar();`
    2. getchar函数不会在读取时跳过空白字符（把空格、回车、Tab键这些看不见的空白字符，也当作一个普通字符来读取，不会自动忽略它们。）
    3. getchar常与while配合，编写需从键盘输入字符的程序  

| 区别点 | getchar() | scanf() |
|:--|:--|:--|
| **1. 读取数据类型与数量** | 只能读取 **1 个字符**，返回该字符的 **ASCII 码** | 可按格式符（如 `%d`、`%s`、`%c`）读取 **多种数据类型**，一次可读取多个数据 |
| **2. 对空白字符的处理** | **不跳过空白字符**（空格、回车、制表符等都会被当作输入） | **除 `%c` 外**，默认会 **自动跳过空白字符**，直接读取有效数据 |
| **3. 返回值含义** | 成功时返回读取字符的 ASCII 码，失败或遇到文件尾返回 **EOF** | 返回 **成功读取的数据项个数**，失败或遇到文件尾返回 **EOF** |

# 三、类型转换  
## 1. 自动类型转换  
  *1.1.* 当运算符的两边出现不一致的类型时，会自动转换成较大的类型大的意思是能表达的数的范围更大 
  - char -> short -> int -> long ->  long long 
  - int -> float -> double
    
*1.2.*  **对于 printf** ,任何小于 int 的类型会被转换成 int ; float 会被转换成 double   
   **但是 scanf 不会**,要输入 short需要% hd  

## 2. 强制类型转换  
通常要把一个量强制转换成另一个类型（通常是较小的类型）
如`( int )10.2 
( short )32`
注意这时候的安全性，小的变量不总能表达大的量 
`( short )32768`

```c
#include<stdio.h>
int main()
{
printf("%d\n",(short)32768);
return 0;
}
```
结果`-32768`

- 强制类型转换只是从那个变量计算出了一个新的类型的值，它并不改变那个变量，无论是值还是类型都不改变  
- 强制类型转换的优先级高于四则运算`int i = (int)(a / b)`

# 四、 unsigned & signed
---

## ✅ 1. 是否允许负数

| 类型               | 能不能是负数     | 例子               |
| ---------------- | ---------- | ---------------- |
| `int`（默认 signed） | ✔ 可以       | -10、0、100        |
| `unsigned int`   | ✘ 不可以，只能≥0 | 0、100、4294967295 |

也就是说，**有 unsigned = 永远非负**。

---

## ✅ 2. 数值范围不同（32 位机器举例）

| 类型             | 范围                              |
| -------------- | ------------------------------- |
| `int`          | -2,147,483,648 ～ +2,147,483,647 |
| `unsigned int` | 0 ～ 4,294,967,295               |

所以：
➤ **unsigned 的最大值大约是 signed 的两倍，但不能表示负数。**

---

## ✅ 3. 数值溢出（超范围）时表现不同

#### 🧪 signed int 溢出（结果未定义，危险）

```c
int x = 2147483647; // 最大值
x = x + 1;          // 可能变负，也可能出错 → undefined behavior
```

#### 🧪 unsigned 溢出（按模 2^n 循环，不会 UB）

```c
unsigned int x = 4294967295; // 最大值
x = x + 1;   // 结果变为 0（自动回绕）
```

**unsigned 更安全，不会出现未定义行为（但逻辑需小心）。**

---

## ✅ 4. 比较时的“坑”

如果一个是 unsigned，一个是 signed，会自动转换为 *unsigned*，可能导致错判。

例子：

```c
int a = -1;
unsigned int b = 1;

if (a < b)
```

你以为：
-1 < 1 → true?

实际：
`a` 被自动转成 *巨大的 unsigned 整数*（比如 4294967295），所以：

👉 **判断结果是 false！**

这是 C 中非常常见的 bug。

---

## ✔ 用最简单的一句话总结

> **unsigned 表示更大的正数范围，但不能为负；signed 可以为负。混用 signed 和 unsigned 时会出现隐式转换坑。**





